{
  "scripts": {
    "build": "bash build.sh || (mkdir -p public && cp -r * public/ || true && echo 'Fallback build completed')"
  },
  "dependencies": {
    "@rainbow-me/rainbowkit": "^2.2.4",
    "viem": "^2.27.2",
    "wagmi": "^2.14.16"
  }
}
